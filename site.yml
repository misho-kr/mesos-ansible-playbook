---
# Build Mesos cluster and frameworks

- hosts: mesos

  pre_tasks:
  - include: tasks/prerequisites.yml

  roles:
  - zookeeper
#  - { role: mesos,              tags=docker }

  post_tasks:
  - include: tasks/package.yml
